# Sample Data

This folder is reserved for optional local CSV samples.

The app can also load `/mnt/data/模拟用客户数据.csv` when that file exists, but no dataset schema is hard-coded into the application.

